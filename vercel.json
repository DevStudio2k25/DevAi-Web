{
  "version": 2,
  "installCommand": "git clone https://github.com/flutter/flutter.git --depth 1 -b stable /opt/flutter && export PATH=\"$PATH:/opt/flutter/bin\" && flutter precache && flutter config --enable-web",
  "buildCommand": "export PATH=\"$PATH:/opt/flutter/bin\" && flutter build web --release",
  "outputDirectory": "build/web",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=0, must-revalidate"
        }
      ]
    }
  ]
} 